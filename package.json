{
  "name": "http-port-guard",
  "version": "1.0.0",
  "description": "Wrap an HTTP server 'start' function in asynchronous checks for port availability.",
  "main": "src/index.js",
  "scripts": {
    "lint": "jshint src test",
    "test": "npm run lint && npm run test-unit",
    "test-unit": "node test/test"
  },
  "author": "Mike Pennisi",
  "license": "MIT",
  "devDependencies": {
    "jshint": "^2.8.0",
    "sinon": "^1.17.1",
    "tape": "^4.2.1"
  }
}
